{
  "name": "new",
  "version": "0.1.0",
  "private": true,
  "devDependencies": {
    "gh-pages": "^6.3.0"
  },
  "dependencies": {
    "bootstrap": "^5.1.3",
    "next": "^14.0.0",
    "react": "^18.0.0",
    "react-bootstrap": "^2.1.2",
    "react-dom": "^18.0.0",
    "react-pdf": "^5.7.0"
  },
  "scripts": {
    "dev": "next dev",
    "build": "next build",
    "start": "next start",
    "deploy": "next build && touch out/.nojekyll && gh-pages -d out --dotfiles"
  }
}
